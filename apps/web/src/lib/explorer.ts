/**
 * Block explorer URL utilities - re-exported from @swr/chains.
 */

// Re-export all explorer utilities from @swr/chains
export {
  getExplorerAddressUrl,
  getExplorerTxUrl,
  getExplorerName,
  getChainName,
  getChainShortName,
  getBridgeExplorerName,
  getBridgeMessageUrl,
  getBridgeMessageByIdUrl,
  getLocalChainIds,
} from '@swr/chains';
