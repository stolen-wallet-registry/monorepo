# Foundry build artifacts
/out
/cache

# Broadcast files (deployment records)
/broadcast

# Dependencies (restored via forge install)
/lib

# Gas snapshots (regenerate with: pnpm report:gas)
# Keep tooling-owned to reduce noisy diffs
.gas-snapshot
